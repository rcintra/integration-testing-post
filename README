Global setup:

 Download and install Git
  git config --global user.name "Rafael"
  git config --global user.email ra.cintra@gmail.com
        

Next steps:

  mkdir integration-testing-post
  cd integration-testing-post
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:rcintra/integration-testing-post.git
  git push origin master
      

Existing Git Repo?

  cd existing_git_repo
  git remote add origin git@github.com:rcintra/integration-testing-post.git
  git push origin master
      